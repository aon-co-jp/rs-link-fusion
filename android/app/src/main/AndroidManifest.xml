<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- WiFi/USB-Ethernetの2回線を`ConnectivityManager.requestNetwork()`で
         同時保持するために必要(NetworkBinder.kt参照)。 -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- USB-Ethernetアダプタの抜き差しをアプリ起動トリガーにする場合に
         備えて宣言(今回のスコープでは自動起動UIまでは実装していない、
         正直な開示——CLAUDE.md HANDOFF参照)。 -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RsLinkFusion"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
